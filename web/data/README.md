# Generated visualization data

Run `uv run single-stair visualize export` from the repository root. The export joins the official
ward boundary snapshot to ward-level three-bedroom capacity aggregates and generates the parcel,
neighborhood, and comparison datasets from the latest analytical snapshots. Generated outputs are
intentionally not committed.
