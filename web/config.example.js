window.SINGLE_STAIR_CONFIG = {
  // Browser map keys are public. Restrict this key to your deployed domains in Protomaps.
  protomapsApiKey: "",
  // Use a self-hosted Protomaps PMTiles URL or a Protomaps-compatible ZXY vector endpoint.
  // PMTiles example: "https://your-domain.example/chicago.pmtiles"
  // Endpoint example: "https://your-domain.example/{z}/{x}/{y}.mvt"
  protomapsUrl: ""
};
